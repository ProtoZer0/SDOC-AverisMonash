

# Dataset bundle is vendored for the live deploy; keep the attachments, not the noise
.DS_Store
__pycache__/
*.py[cod]
.venv/
venv/
.env
*.log

# eval artefacts that are regenerated
.cache/gemini/
eval/.cache/

# frontend
node_modules/
dist/
.serena/
.codex-build/
.chart-data-*/
.tools/

# The organisers' scoring package. Holds ground_truth.json and the dataset
# generator, so it never belongs in a public repo. Keep it on disk to run the
# scorer; it is excluded from the image by .dockerignore.
sdoc-hackathon-docker/
